export global Tasks {
    in-out property <[string]> name;
    // 0: Success, 1: Failed, 255: Processing
    in-out property <[int]> status;
    in-out property <[string]> result;
    in-out property <int> index;
}
